{
  "name": "my-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite dev",          // 開発サーバー起動
    "build": "vite build",      // 静的ファイルビルド
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^4.0.0",
    "gh-pages": "^5.0.0"
  }
}